# Django settings for Docker
DJANGO_SECRET_KEY=change-me-in-production
DJANGO_DEBUG=True
# Comma-separated hosts for Django ALLOWED_HOSTS.
# Update for localhost, Raspberry Pi hostname, and LAN IP before running.
ALLOWED_HOSTS=localhost,127.0.0.1

# Frontend exposed port on the host
FRONTEND_PORT=8080
